# スクレイピング設定
# SCRAPE_USER_AGENT=Mozilla/5.0 (compatible; ScrapeLab/1.0)
# SCRAPE_TIMEOUT_MS=10000
# TLS 証明書検証を無効化 (ローカル開発・自己署名証明書対応, 本番では使用しないこと)
SCRAPE_TLS_REJECT_UNAUTHORIZED=false

# 将来: Playwright リモートブラウザ
# PLAYWRIGHT_WS_ENDPOINT=ws://localhost:3001

# 将来: 結果永続化
# DATA_DIR=./data
